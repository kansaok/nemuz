name: CI

on:
  push:
    branches: [master]
    tags: ["v*"]
  pull_request:

permissions:
  contents: read

env:
  # Must match the go directive in go.mod. The check step below enforces that,
  # because a dependency can raise it — modernc.org/sqlite did — and without
  # the check the only job that notices is the offline one, hours later.
  GO_VERSION: "1.25"
  # Without this, Go silently downloads whatever toolchain go.mod asks for, so
  # CI tests a version it never names. The offline job cannot download one, so
  # a mismatch showed up there and nowhere else.
  GOTOOLCHAIN: local

jobs:
  test:
    name: Build and test
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          check-latest: true

      - name: Go version matches go.mod
        run: |
          required=$(go mod edit -json | python3 -c 'import json,sys; print(json.load(sys.stdin)["Go"])')
          if [ "${required%.*}" != "$GO_VERSION" ] && [ "$required" != "$GO_VERSION" ]; then
            echo "go.mod requires Go $required but CI runs $GO_VERSION."
            echo "A dependency probably raised it. Update GO_VERSION here and the README."
            exit 1
          fi
          go version

      - name: Formatting
        run: |
          unformatted=$(gofmt -l .)
          if [ -n "$unformatted" ]; then
            echo "These files are not gofmt'd:"
            echo "$unformatted"
            exit 1
          fi

      - name: Vet and size budget
        run: make lint

      - name: Test
        run: go test -race ./...

      - name: Build
        run: make build

      - name: Size and startup budgets
        run: make bench

  offline:
    name: Tests need no network
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          check-latest: true

      # nemuz claims its whole suite runs without touching the network: every
      # provider test uses httptest, and every replay reads a recording. This
      # job makes that a fact rather than an intention.
      #
      # The isolation is a container with no network interface but loopback,
      # rather than a firewall rule on the runner. Blocking egress on the runner
      # also cuts the runner's own connection to GitHub, so the job hangs
      # instead of reporting — which is exactly what the first version of this
      # file did.
      - name: Fetch dependencies before isolating
        run: go mod download

      - name: Run the suite with no network
        run: |
          docker run --rm --network none \
            --user "$(id -u):$(id -g)" \
            -v "$PWD:/src" -w /src \
            -v "$(go env GOMODCACHE):/gomodcache" \
            -e GOMODCACHE=/gomodcache \
            -e GOCACHE=/tmp/gocache \
            -e HOME=/tmp \
            "golang:${GO_VERSION}" \
            go test -count=1 ./...

      - name: Confirm the container really had no network
        run: |
          if docker run --rm --network none "golang:${GO_VERSION}" \
             timeout 5 curl -sSf https://proxy.golang.org > /dev/null 2>&1; then
            echo "the isolated container reached the internet; this job proves nothing"
            exit 1
          fi
          echo "confirmed: no outbound traffic is possible in that container"

  sandbox:
    name: Sandbox is enforced
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          check-latest: true

      - name: Build
        run: make build

      # doctor spawns the tool worker and has it attempt a read outside its
      # workspace. A build that computes the right capability grants and never
      # applies them passes every other job and fails this one.
      - name: Verify tool confinement
        run: |
          ./bin/nemuz doctor
          ./bin/nemuz doctor | grep -q "sandbox    verified" || {
            echo "doctor did not verify tool confinement"
            exit 1
          }
